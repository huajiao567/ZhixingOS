# 项目状态

最后更新：2026-09-07

## 当前结论

知行镜已经形成可运行的个人智能环境主干，而不是仅有页面的原型：自然输入可进入统一意图和生命对象，执行前有权限与确认，执行后有真实回执、部分失败和撤回；证据可进入分层记忆与可纠正的孪生档案；“时、位、势、应、变、中”已成为行动比较内核；电脑与手机之间已有显式、用户隔离、可消费/取消、会过期的工作接力；Android、Web 和后端均有可重复构建路径。

它仍不是生产完成态。物理 Android/iOS 矩阵、正式签名/AAB、原生生活数据适配器、跨设备冲突、端到端加密和公共实例治理仍需真实世界投入，不能用接口、文档或 Web 测试代替。

## 能力矩阵

| 能力域 | 当前状态 | 已完成 | 下一道真实门槛 |
|---|---|---|---|
| 执行秘书与开放工作台 | implemented-v1 | 任务、日程、课程、记录、草稿的统一解析、确认、执行、回执、撤回 | 更广泛自然语言、A4 预授权自治、中断恢复、更多真实执行器 |
| 分层记忆与数字孪生 | implemented-v1 | L0-L3 召回预算、来源与版本；证据阈值、反例、纠正、回滚 | 30 天以上衰减/合并、跨设备一致性、长期用户研究 |
| 易理变化内核 | implemented-v1 | 时位势应变中、替代路径、风险、停止条件、复审时间 | 阶段迁移与张力图、现实反馈校准、纵向有效性研究 |
| 3D 数字人 | implemented-v1 | 真实 VRM/GLB、动态表情/姿态、照片拟合、阶段时间线、生活信号限幅适应 | 物理设备视觉质量、不同模型骨骼兼容、长期养成验证 |
| 生活数据 | contract-complete / adapters-pending | Health Connect/小米、手机使用、ActivityWatch、饮食照片/条码的统一协议、权限和隐私边界 | 各平台原生模块、真实数据去重与同步矩阵 |
| 数据主权 | implemented-v1 | 来源授权、审计、导出、撤回、按来源软删除与派生重算 | 加密数据包、备份删除 SLA、部署地区合规验证 |
| 桌面工作台 | implemented-v1 | ≥1100px Web 进入独立宽屏工作台；承诺/项目/实验并列、统一工作台快速入口、键盘快捷键、共享后端对象与同步状态 | 原生桌面壳、文件/窗口连接器、系统级快捷动作 |
| 跨设备连续性 | implemented-v1 | 电脑↔手机双向文本接力；服务端用户隔离、24h TTL、open/consumed/cancelled 状态、刷新与显式继续；不冒充剪贴板 | 原生深链/后台通知、离线接力队列、设备身份与冲突合并 |
| Android 交付 | test-apk | SDK 36、四 ABI、bundled ML Kit、v2 debug 签名、可侧载 APK | 物理 Android 安装/权限/离线矩阵、production keystore、AAB |
| 开源工程 | release-candidate | MIT、贡献/治理/安全/隐私文档、依赖与候选文件审计 | 仓库 URL、维护者联系人、历史凭据扫描、首个可复现 tag |

## 三个核心思想的融合

- 秘书不是聊天外壳：它通过 `IntentGraph → LifeObject → ActionGateway → ActionReceipt` 改变真实工作台，并在信息不足时提问。
- 数字孪生不是评分画像：身份、用户确认外观、临时状态、生活数据适应和长期成长分层保存。熬夜可形成会衰减的眼下疲劳；压力只形成紧绷；单餐不改变长期体型。
- 易理不是占卜文案：“时、位、势、应、变、中”进入路径评估、可逆性、停止条件和复审时间；现实结果回流证据与记忆。
- 三者已形成 v1 闭环：输入和设备摘要可影响计划、行动回执、证据、记忆与孪生表现；但长期自治与真实设备连接器仍未完成。

## 当前自动化证据

- 前端领域测试：41/41。
- 后端契约/记忆测试：7/7。
- 前后端 TypeScript：通过。
- Expo Doctor：21/21；Web production export：通过。
- 1440 × 960 桌面工作台 E2E：1/1，覆盖宽屏入口、键盘聚焦、统一执行内核跳转和进程导航。
- 电脑↔手机连续性 E2E：1/1，在同一真实后端上使用 1440 × 960 与 390 × 844 两个独立浏览器上下文，验证创建、发现、消费、反向接力与消费后消失。
- 390 × 844 触摸视口 E2E：3/3，覆盖生活信号到 3D 解释、数据主权、错误登录和工作台执行/撤销。
- Android release 编译与 v2 debug 签名校验：通过；APK 字节与 SHA-256 见 [ANDROID_APK_TEST.md](ANDROID_APK_TEST.md)。
- 开源候选审计：205 个文件，未发现候选密钥、数据库、APK、私钥或超大文件。

## 已知边界

- 当前 APK 是 debug certificate 侧载测试包，不是商店生产包。
- 当前 Windows 主机未启用固件虚拟化且没有连接 Android 真机，不能声称原生 UI、相机、录音、日历、Health Connect 和 3D 视觉已在真机通过。
- 当前跨端连续性证据来自两个独立 Web 浏览器上下文共享真实后端；它证明协议与 UI 链路，不证明 Android/iOS 后台通知、系统剪贴板、原生深链或物理设备网络切换已经通过。
- 生活数据连接器协议和处理引擎已完成；除本人文字自述外，外部连接器仍需相应原生模块、平台 entitlement 或桌面伴侣，界面不得把授权开关显示成“已同步数据”。
- 健康、饮食和压力表现不是医疗判断；任何高影响建议仍需专业人士和用户确认。

## 后续升级提案

- [福利型社会·人人寻找意义升级提案](proposals/2026-08-26-welfare-society-meaning-upgrade.md)：意义罗盘、探索者模式、毕业与回归等方向，尚未实施。
